# --- licensed / large data (never commit) ---------------------------------
# Everything under data/raw/ and data/processed/ is derived from MIMIC-III
# (credentialed DUA - must never be redistributed) or is regenerated by
# scripts/demo_mode.ps1 (public demo). Only data/mappings/ (TWOSIDES / PubChem
# side tables, no patient data) and data/README.md are tracked.
data/raw/
data/raw/**
data/processed/
data/processed/**
data/processed/mimic3/**
data/processed/demo/**
*.csv.gz
twosides.csv
# processed artefacts anywhere (records.pkl carries SUBJECT_IDs)
records.pkl
graph.pt
voc.json
drug_raw_names.json

# --- checkpoints & scratch results -----------------------------------------
results/runs/**/best.pt
results/runs/**/*.pt
results/runs/**/*.pth
results/_superseded*/
results/ablation_stdout.log
results/ablation_stderr.log
results_demo/
checkpoints/
*.pth
*.ckpt

# --- python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.venv*/
venv/
.mypy_cache/
.ruff_cache/

# --- editors / OS -------------------------------------------------------------
.vscode/
.idea/
Thumbs.db
.DS_Store

[pytest]
testpaths = tests
# faulthandler is disabled because a broken optional torch_scatter DLL in some
# environments triggers a (harmless, caught) Windows SEH exception at import
# time that faulthandler would otherwise print as a scary traceback.
addopts = -p no:faulthandler -W ignore::DeprecationWarning -W ignore::UserWarning

# A0 - full model (reference). Training writes best.pt when keep_checkpoint is true;
# checkpoints are git-ignored and not shipped. Regenerate with:
#   python scripts/train.py --config configs/ablation/full.yaml --seed 0
base: _base.yaml
variant: full
training:
  keep_checkpoint: true
